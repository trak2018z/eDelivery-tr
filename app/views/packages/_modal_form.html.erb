<div class="modal-body">
  <div class="form-group">
    <row id="package_size">
      <div class="form-inline">
        <div class="title-div col-12" hidden="true">
          <button type="button" class="close rm-package" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <input type="number" min="0" price-for="<%= price_of_package_element[:weight] %>" class="form-control col-12 col-sm-6 col-md-3 modal-form-fields" id="weight" name="order[packages][index][weight]" placeholder="Weight" required="required">
        <input type="number" min="0" price-for="<%= price_of_package_element[:height] %>" class="form-control col-12 col-sm-6 col-md-3 modal-form-fields" id="height" name="order[packages][index][height]" placeholder="Height" required="required">
        <input type="number" min="0" price-for="<%= price_of_package_element[:width] %>" class="form-control col-12 col-sm-6 col-md-3 modal-form-fields" id="width" name="order[packages][index][width]" placeholder="Width" required="required">
        <input type="number" min="0" price-for="<%= price_of_package_element[:length] %>" class="form-control col-12 col-sm-6 col-md-3 modal-form-fields" id="length" name="order[packages][index][length]" placeholder="Length" required="required">
      </div>
    </row>
    <span class="text-muted small pd-1">Define in centimeters.</span>
  </div>
</div>

<div class="modal-footer">
  <div class="form-inline">
    <div class="input-group col-12 col-sm-5 mb-2 px-0">
      <span class="input-group-addon"><%= t('currency.short') %></span>
      <input class="form-control" id="price_modal" value="0.00" disabled>
    </div>
    <div class="text-right col-12 col-sm-7 pt-0 px-0">
      <button type="button" class="btn btn-secondary col-12 col-sm-5 mb-2" data-dismiss="modal">Cancel</button>
      <button id="add_package" type="button" class="btn btn-primary col-12 col-sm-6 mb-2">Add package</button>
    </div>
  </div>
</div>